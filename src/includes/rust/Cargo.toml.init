[package]
name = "{{ project }}"
version = "{{ version }}"
authors = ["{{ name }} <{{ email }}>"]
excludes = [ ".gitignore" ]

[profile.release]
lto = true

[[bin]]
name = "{{ project }}"
path = "src/main.rs"

[dependencies]
